class Item {
  String title, id, image;
  int countView, count;
  num priceView, price;
  Item(
      {this.title,
      this.price,
      this.id,
      this.count,
      this.image,
      this.countView,
      this.priceView});
}
